<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1>Car List</h1>
      <%= render 'public/searches/form', cars: @cars %> 
      <h2>
        <%= link_to '新規投稿', href="/maintenances/new", class: 'btn btn-primary' %>
        <%= link_to '所有車登録', href="/cars/new", class: 'btn btn-secondary' %>
      </h2>
      <%= render 'index', cars: @cars %>
    </div>
  </div>
</div>